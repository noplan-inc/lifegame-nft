import React from 'react';
import { Collections } from '../container/collections';

export const Collection = () => {
    return (
        <div>
            <h2>Collection</h2>
            <Collections />
        </div>
    );
};
